<nav class="navbar navbar-expand-sm navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">
            <img src="/assets/images/Idealjob.png" class="img-fluid rounded" id="logo" alt="logo">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown"
                aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0 col-2 justify-content-between">
                <li class="nav-item">
                    <a class="nav-link active fs-5" aria-current="page" href="/accueil">Accueil</a>
                </li>
            </ul>
            <ul class="navbar-nav me-auto mb-2 mb-lg-0 col-3 justify-content-between">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="">
                        <img src="/assets/images/stickeridealjob.png" class="img-fluid rounded" id="sticker" alt="sticker">
                    </a>
                </li>
            </ul>
            <div class="fs-5">
                {% if (session.user.profil_github) %}
                    <img class="github-avatar"
                         src="https://avatars.githubusercontent.com/{{ session.user.profil_github }}" alt="avatar"
                         id="avatar">
                    {{ session.user.firstname }}
                {% elseif session.user %}
                    <i class="fab fa-github fa-3x"></i>
                {% endif %}
            </div>
            <ul class="navbar-nav mb-2 mb-lg-0">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                       data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="fas fa-user fa-2x"></i>
                    </a>
                    <ul class="dropdown-menu col-2 fs-5" aria-labelledby="navbarDropdown">
                        {% if session.user %}
                            <li><a class="dropdown-item" href="/profil">Mon compte</a></li>
                            <li><a class="dropdown-item" href="logout">Déconnexion</a></li>
                        {% else %}
                            <a class="dropdown-item" href="/">Connectez-vous</a>
                        {% endif %}
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>
