<nav class="navbar navbar-expand-sm navbar-light bg-light fixed-top">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">
            <img src="/assets/images/Idealjob2.png" class="img-fluid border border-danger rounded" id="logo" alt="logo">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown"
                aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0 col-2 justify-content-between">
                <li class="nav-item">
                    <a class="nav-link active fs-5" aria-current="page" href="/accueil"><i class="fas fa-home"></i> Accueil</a>
                </li>
            </ul>
            <div class="fs-5">
                {% if (session.user.profil_github) %}
                    <img class="github-avatar m-2"
                         src="https://avatars.githubusercontent.com/{{ session.user.profil_github }}" alt="avatar"
                         id="avatar">
                    {{ session.user.firstname }}
                {% elseif session.user %}
                    <i class="fab fa-github fa-2x"></i>
                {% endif %}
            </div>
            <ul class="navbar-nav mb-2 mb-lg-0">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                       data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="usernav fas fa-user fa-2x"></i>
                    </a>
                    <ul class="dropdown-menu col-3 fs-4 border border-danger" aria-labelledby="navbarDropdown">
                        {% if session.user %}
                            <li><a class="dropdown-item" href="/profil">Mon compte <i class="far fa-chart-bar ms-2"></i></a></li>
                            {% if session.user.is_admin %}
                                <li><a href="/admin" class="dropdown-item">Administration<i class="fontdropadmin fas fa-user-lock ms-1"></i></a></li>
                            {% endif %}
                            <li><a class="dropdown-item" href="logout">Déconnexion <i class="fas fa-sign-out-alt ms-2"></i></a></li>
                        {% else %}
                            <a class="dropdown-item" href="/">Connectez-vous</a>
                        {% endif %}
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>
